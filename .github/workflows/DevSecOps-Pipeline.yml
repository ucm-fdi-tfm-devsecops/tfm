name: DevSecOps Pipeline

on:
  workflow_dispatch:
    inputs:
      registry:
        description: "Container registry (ghcr.io, docker.io, ...)"
        required: true
        default: "ghcr.io"
      image_name:
        description: "Image name (org/repo o usuario/imagen)"
        required: true
        default: "your-org/your-app"
      image_tar:
        description: "Nombre del artifact de la imagen"
        required: true
        default: "docker-image.tar"

permissions:
  id-token: write
  contents: read
  packages: write
  security-events: write

jobs:
  # 1) Secrets
  secrets-scan:
    uses: ./.github/workflows/Secrets_Scan.yml
    secrets: inherit

  # 2) SCA/SAST/IaC (en paralelo) — todos dependen de Secrets
  sca:
    needs: secrets-scan
    uses: ./.github/workflows/SCA.yml
    secrets: inherit

  sast:
    needs: secrets-scan
    uses: ./.github/workflows/SAST.yml
    secrets: inherit

  iac-security:
    needs: secrets-scan
    uses: ./.github/workflows/IaC_Security.yml
    secrets: inherit

  # 3) Docker build — espera a SCA + SAST + IaC (como en la imagen)
  docker-build:
    needs: [sca, sast, iac-security]
    uses: ./.github/workflows/Docker_Build.yml
    with:
      registry:  ${{ github.event.inputs.registry }}
      image_name: ${{ github.event.inputs.image_name }}
      image_tag:  ${{ github.sha }}
      image_tar:  ${{ github.event.inputs.image_tar }}
    secrets: inherit

  # 4) Descargar artifact (.tar)
  download-image-artifact-1:
    needs: docker-build
    uses: ./.github/workflows/Download_Docker_Artifact.yml
    with:
      image_tar: ${{ github.event.inputs.image_tar }}
    secrets: inherit

  # 5) Image scan
  image-scan:
    needs: download-image-artifact-1
    uses: ./.github/workflows/ImageScan.yml
    with:
      registry:  ${{ github.event.inputs.registry }}
      image_name: ${{ github.event.inputs.image_name }}
      image_tag:  ${{ github.sha }}
      # si tu ImageScan.yml NO define este input, no lo pongas:
      # exit_code_on_findings: 0
    secrets: inherit

  # 6) Descargar de nuevo el artifact para firmar
  download-image-artifact-2:
    needs: image-scan
    uses: ./.github/workflows/Download_Docker_Artifact.yml
    with:
      image_tar: ${{ github.event.inputs.image_tar }}
    secrets: inherit

  # 7) Container signing
  container-signing:
    needs: download-image-artifact-2
    uses: ./.github/workflows/Container_Signing.yml
    with:
      registry:  ${{ github.event.inputs.registry }}
      image_name: ${{ github.event.inputs.image_name }}
      image_tag:  ${{ github.sha }}
    secrets: inherit

  # 8) Push firmado
  push-signed:
    needs: container-signing
    uses: ./.github/workflows/Push_Signed.yml
    with:
      registry:  ${{ github.event.inputs.registry }}
      image_name: ${{ github.event.inputs.image_name }}
      image_tag:  ${{ github.sha }}
    secrets: inherit

