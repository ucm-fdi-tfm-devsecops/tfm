name: Step 2.2 - SAST

on:
  workflow_dispatch:
  workflow_call:

jobs:
  semgrep-sast:
    name: Semgrep SAST Analysis
    runs-on: ubuntu-latest

    permissions:
      contents: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run Semgrep SAST
        run: |
          mkdir -p results
          # Escanea todo el repo, no una carpeta fija
          semgrep scan --config auto --sarif . > results/semgrep-results.sarif || echo '{}' > results/semgrep-results.sarif

      - name: Upload SARIF results as artifact
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-sast-results
          path: results/semgrep-results.sarif

      - name: Upload SARIF to GitHub Security Dashboard
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results/semgrep-results.sarif
